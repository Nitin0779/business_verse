# Trained ML models are saved here by the app.
# Files generated:
#   sales_model.pkl       — Linear Regression for sales forecasting
#   churn_model.pkl       — Random Forest for churn prediction
#   churn_scaler.pkl      — StandardScaler for churn model
#   kmeans_model.pkl      — K-Means for customer segmentation
#   kmeans_scaler.pkl     — StandardScaler for K-Means
